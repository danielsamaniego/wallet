{
  "version": 2,
  "regions": ["iad1"],
  "builds": [
    { "src": "api/index.ts", "use": "@vercel/node", "config": { "maxDuration": 55 } }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "api/index.ts" }
  ],
  "crons": [
    {
      "path": "/internal/cron/expire-holds",
      "schedule": "* * * * *"
    },
    {
      "path": "/internal/cron/cleanup-idempotency",
      "schedule": "0 * * * *"
    }
  ]
}
