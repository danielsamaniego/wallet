# Progress — Wallet Service

## What Works

- [x] Project initialized (pnpm, TypeScript strict, Biome, Vitest)
- [x] Docker Compose (PostgreSQL 16 for local dev)
- [x] Dockerfile (multi-stage Node.js)
- [x] pnpm scripts for all workflows (`start:local`, `reset:local`, `db:update`, etc.)
- [x] Utils: AppError (Kind + Code + Message) in `utils/kernel/appError.ts`
- [x] Utils: IIDGenerator interface (`utils/application/id.generator.ts`) + UUID v7 adapter (`utils/infrastructure/uuidV7.ts`)
- [x] Utils: AppContext + createAppContext factory (`utils/kernel/context.ts`) + HonoVariables types (`utils/infrastructure/hono.context.ts`)
- [x] Utils: ILogger port (`utils/kernel/observability/logger.port.ts`) with canonical log support
- [x] Utils: SafeLogger (never throws) in `utils/infrastructure/observability/safe.logger.ts`
- [x] Utils: SensitiveKeysFilter (omits sensitive keys) in `utils/infrastructure/observability/sensitive.filter.ts`
- [x] Utils: CanonicalAccumulator (request-scoped meta + counters) in `utils/kernel/observability/canonical.ts`
- [x] Utils: PinoAdapter (structured JSON logging with context fields) in `utils/infrastructure/observability/pino.adapter.ts`
- [x] CQRS bus: ICommandBus/IQueryBus interfaces (`utils/application/cqrs.ts`) and implementations (`utils/infrastructure/cqrs.ts`) with middleware pipeline
- [x] TransactionManager: Serializable isolation + internal retry (3 attempts, exponential backoff) in `utils/infrastructure/prisma.transaction.manager.ts`
- [x] Middleware: trackingCanonical (tracking_id UUID v7 + canonical dispatch) in `utils/middleware/trackingCanonical.ts`
- [x] Middleware: requestResponseLog (request/response logging) in `utils/middleware/requestResponseLog.ts`
- [x] Middleware: apiKeyAuth (API key validation) in `utils/middleware/apiKeyAuth.ts`
- [x] Middleware: idempotency (duplicate prevention) in `utils/middleware/idempotency.ts`
- [x] Error handling: errorResponse + httpStatus + validationHook (`utils/infrastructure/hono.error.ts`)
- [x] Global onError handler (maps AppError → HTTP status, catches unhandled exceptions)
- [x] Prisma schema (all models)
- [x] Immutable ledger SQL (trigger + constraints)
- [x] Hono app with health endpoint, cors (hono/cors), secureHeaders (hono/secure-headers), notFound, basePath("/v1"), route() sub-apps
- [x] Config from env vars
- [x] Centralized DI wiring (repos + use cases + bus registration in wiring.ts)
- [x] HTTP handlers using handlerFactory.createHandlers() with hono-openapi validator (type-safe, no try/catch)
- [x] HTTP handlers dispatch via commandBus/queryBus (not individual handler instances)
- [x] Tracking middleware supports external X-Tracking-Id header (ext- prefix)
- [x] Auto-generated OpenAPI 3.1 spec via hono-openapi (`/openapi` endpoint)
- [x] Interactive Scalar API reference UI (`/docs` endpoint)
- [x] describeRoute() with tags, summary, and response schemas on all 13 endpoints
- [x] Endpoint schemas.ts files: request schemas (Param, Body, QueryParams) + ResponseSchema per endpoint
- [x] Shared ErrorResponseSchema in `utils/infrastructure/hono.error.ts`
- [x] Reusable listing system: flat Stripe-style filters, dynamic multi-field sorting, keyset cursor pagination
- [x] Listing modules: `utils/kernel/listing.ts` (domain types + cursor encode/decode), `utils/infrastructure/listing.zod.ts` (schema factory), `utils/infrastructure/listing.prisma.ts` (query builder)
- [x] Composite indexes for common filter+sort patterns (Transaction, LedgerEntry)
- [x] Full documentation set
- [x] AGENTS.md
- [x] CLAUDE.md importing `AGENTS.md`

## Implemented

- [x] Wallet bounded context: domain (aggregate, value objects, errors, Movement entity)
- [x] Wallet bounded context: app (command use cases: createWallet, deposit, withdraw, transfer, placeHold, captureHold, voidHold, freeze, unfreeze, close, expireHolds) — each with `command.ts` + `usecase.ts`
- [x] Wallet bounded context: app (query use cases: getWallet, getTransactions, getLedgerEntries) — each with `query.ts` + `usecase.ts`
- [x] Wallet bounded context: application ports (IWalletReadStore, ITransactionReadStore, ILedgerEntryReadStore in `wallet/application/ports/`)
- [x] Wallet bounded context: outbound adapters (Prisma repositories, read stores, Movement repo in `wallet/infrastructure/adapters/outbound/prisma/`)
- [x] Wallet bounded context: inbound HTTP adapters (route files `wallets.routes.ts`, `transfers.routes.ts`, `holds.routes.ts` + per-endpoint handler/schemas folders in `wallet/infrastructure/adapters/inbound/http/`)
- [x] Movement entity (journal entry) for true double-entry ledger grouping
- [x] Scheduled jobs as inbound adapters: hold expiration (`wallet/infrastructure/adapters/inbound/scheduler/expireHolds.job.ts`, 30s interval) and idempotency cleanup (`common/idempotency/infrastructure/adapters/inbound/scheduler/cleanupIdempotency.job.ts`, 60s interval) — both dispatch commands via CommandBus
- [x] Concurrency audit: PlaceHold + VoidHold participate in optimistic locking
- [x] CaptureHold validates real wallet balance
- [x] Expired holds excluded from available_balance queries
- [x] Idempotency: transient error release, payload mismatch detection, method+path scoping
- [x] Architecture refactoring: `shared/` → `utils/` (toolkit) + `common/` (cross-cutting features)
- [x] CQRS bus refactoring: CommandBus/QueryBus with middleware pipeline, static TYPE for dispatch
- [x] Common: idempotency feature with full architecture (ports, command, use case, scheduler adapter, Prisma adapter)
- [x] E2E coverage for balance adjustments endpoint (`tests/e2e/wallet/adjustments.e2e.test.ts`) including success, hold-aware insufficient funds, frozen/closed states, auth, validation, idempotency, cross-tenant isolation, concurrency, and ledger assertions
- [x] Multi-currency support: explicit currency catalog (USD, EUR, MXN, CLP, KWD) with `wallets_supported_currency` CHECK constraint in PostgreSQL
- [x] Renamed all `_cents` fields to `_minor` across domain, application, infrastructure, API, and documentation to accurately reflect multi-currency minor unit semantics

## Implemented (continued)

- [x] `allow_negative_balance` per-platform flag: Platform aggregate field + DB trigger (`trg_enforce_positive_balance`) replacing CHECK constraint + `PATCH /v1/platforms/config` endpoint + available balance sign-correct readstore
- [x] `AdjustBalanceCommand` carries `allowNegativeBalance: boolean`; use case passes to domain; HTTP handler reads from HonoVariables (not AppContext)
- [x] `ImportHistoricalEntry` computes real available balance via `holdRepo.sumActiveHolds()` for negative adjustments; domain receives `allowNegativeBalance=true` to allow negative push, but hold-zombie check still runs (zombie hold = capture permanently fails because `cached < holdAmount` post-import)
- [x] E2E test suite expanded: 221 tests passing (`negative-balance.e2e.test.ts`, `config.e2e.test.ts`, third test platform seeded)
- [x] `Charge` endpoint: `POST /v1/wallets/:walletId/charge` for platform-initiated fees and commissions with optional memo
- [x] Root path redirects to `/docs` for interactive API discovery
- [x] **Distributed lock** (Redis, per-resource): `IDistributedLock` port + `LockRunner` app service + `RedisDistributedLock` adapter (ioredis, `SET NX PX` + token-aware Lua release). Wired on all 12 mutating use cases; transfer sorts+dedupes keys to avoid A↔B deadlock.
- [x] Lock feature toggle via `WALLET_LOCK_ENABLED` + `REDIS_URL`; transparent fallthrough when Redis is unreachable or the feature is off.
- [x] Transient-error classification in the acquire loop: `Command timed out` absorbed and retried within `waitMs` (slow Redis no longer silently degrades the serialization guarantee).
- [x] Pre-lock platform validation in `captureHold`/`voidHold` to prevent cross-tenant DoS via known `holdId`.
- [x] OpenAPI: 409 `LOCK_CONTENDED`/`VERSION_CONFLICT` declared on all 12 mutating endpoints.
- [x] Per-request canonical metrics for observability: `lock.attempts`, `lock.transient_errors`, `lock.token_mismatch`, `lock.acquired`, `lock.contended`, `lock.fallthrough`, `lock.duration_ms`; Redis connection lifecycle events hooked in wiring.
- [x] E2E coverage in `tests/e2e/wallet/wallet-lock.e2e.test.ts`: 50/100 concurrent deposits, cross-wallet parallelism preservation, mixed deposit/withdraw/adjust on same wallet, forced contention via external Redis holder to validate the 409 `LOCK_CONTENDED` wire path.
- [x] **System wallet sharding**: `wallets.shard_index` (NOT NULL, default 0) + `platforms.system_wallet_shard_count` (default 32, 1..1024, only-increase). FNV-1a hash routes each movement to one of N shards; `adjustSystemShardBalance` uses `UPDATE … RETURNING` for a single-statement read+write. All 6 mutation use cases updated; `findSystemWallet`/`adjustSystemWalletBalance` removed.
- [x] `ensureSystemWalletShards` is lazy + idempotent (`findMany` + `createMany(skipDuplicates)`); called outside the SERIALIZABLE tx from `CreateWalletUseCase` to avoid cross-request aborts on shard rows.
- [x] `UpdatePlatformConfig` accepts `systemWalletShardCount`; when it grows, materialises the expanded set for every currency in use on the platform. Domain enforces only-increase.
- [x] Immutable-ledger trigger `prevent_wallet_field_tampering` extended to block `UPDATE` on `shard_index`.
- [x] TransactionManager's `isRetryable` extended to match Prisma 7's `TransactionWriteConflict` class + messages; previously leaked to 500 under heavy cross-wallet load.
- [x] E2E coverage in `tests/e2e/wallet/system-wallet-sharding.e2e.test.ts`: 150 wallets × 4 concurrent deposits each (600 ops) at `>95%` success with zero 500s; ledger zero-sum invariant across all shards; lazy re-materialisation after shard delete; system shards never leak into `/v1/wallets` listing.
- [x] **Async movement processing — Phase 1A (foundation, no behavior change):** `Movement` aggregate gains a `MovementStatus` lifecycle (`pending` | `processing` | `posted` | `failed` | `reversed`) and a nullable `failed_reason` text. Schema migration `20260511060527_add_movement_status_lifecycle` is additive with `DEFAULT 'posted'`, so all existing rows are correct without backfill. `PrismaMovementRepo` persists both new fields. Index `(status, created_at)` added to support future worker draining queries. All 7 synchronous use cases (`deposit`, `withdraw`, `transfer`, `charge`, `adjustBalance`, `captureHold`, `importHistoricalEntry`) continue to call `Movement.create({ id, type, createdAt })` and receive the `posted` default — API contract and runtime behavior are unchanged. `WALLET_ASYNC_PROCESSING_ENABLED=false` everywhere; the flag is not yet read by any code path. See `HIGH_CONCURRENCY_PLAN.md` for the full multi-phase plan.
- [x] **Async movement processing — Phase 1B (read endpoint):** `GET /v1/movements/{id}` returns `{ id, type, status, reason, failed_reason, created_at }`. Backed by `IMovementReadStore` port + `PrismaMovementReadStore` (filters by `transactions.some.wallet.platformId` for cross-tenant isolation; missing or foreign movements both surface as 404 so attackers cannot enumerate ids) + `GetMovementUseCase` + new `movements.routes.ts`. OpenAPI/Scalar pick up the route automatically. E2E coverage in `tests/e2e/wallet/get-movement.e2e.test.ts`: happy path (deposit + adjustment with reason), missing/malformed/SQL-injection-shaped API key, cross-tenant 404, non-existent UUID, oversized id (400), method discipline (POST → 404), info-disclosure invariants on the error body.
- [x] **Async movement processing — Phase 1C (worker scaffolding):** new internal endpoint `POST /internal/worker/process-movement`, mounted outside `/v1` (not part of the public API contract). Authenticated by `Upstash-Signature` JWT verified against an injected `Receiver` from `@upstash/qstash`. New cross-cutting middleware `qstashSignature` in `utils/infrastructure/middleware/` reads the raw body, verifies the signature, and stores the body on `HonoVariables.rawBody` for the downstream handler. `Config.qstash` gains optional `currentSigningKey` / `nextSigningKey`; when either is missing the worker route is not mounted (404 — fail-closed). Local dev (`docker-compose.dev.yml`) and test (`docker-compose.test.yml`) wire the public dev keys. Phase 1C handler is intentionally empty: parses body with Zod (`movement_id` required), logs receipt, returns 200. Phase 2 replaces the body with the real `ProcessMovementUseCase`. Unit: 933/933 at 100% coverage; new tests cover middleware, handler, routes branching (receiver wired vs not), and the four QStash-key config permutations. E2E in `tests/e2e/wallet/worker-process-movement.e2e.test.ts` covers the negative paths (missing/malformed signature, wrong auth scheme, method discipline, info disclosure).
- [x] **Async movement processing — Phase 1D (outbound port declarations):** two new TypeScript interfaces in `src/wallet/domain/ports/` declaring the outbound contracts Phase 2 will wire: `IMovementQueuePublisher` (`publish(ctx, { movementId, idempotencyKey? })` — schedules a pending movement for async processing; payload is intentionally tiny so source of truth stays in Postgres) and `IResultPublisher` (`publish(ctx, { movementId, status: "posted" | "failed", failedReason? })` — used by the worker to notify the awaiting handler via Redis pub/sub for the "sync illusion" pattern). Pure declarations: zero runtime code, zero adapters, zero wiring. Mirrors the existing `IWalletReadStore` / `IMovementRepository` pattern. Phase 2 adds `QStashMovementQueuePublisher` and `RedisResultPublisher` adapters and registers them via `wallet.module.ts`.
- [x] **Async movement processing — Phase 2B.7b (six mutating HTTP handlers refactored behind the rollout flag):** new `IResultSubscriber` domain port at `src/wallet/domain/ports/result.subscriber.ts` so the application layer depends on the interface (`Dependencies.resultSubscriber: IResultSubscriber`; `RedisResultSubscriber implements IResultSubscriber`). New `asyncDispatch` helper at `src/wallet/application/worker/asyncDispatch.ts` encapsulates enqueue+waitFor → returns a discriminated union `{ kind: "completed" | "failed" | "pending", ... }`; defensive fallbacks for missing body or reason; bus errors propagate. New shared `MutationHandlerDeps` type + `buildMutationDeps(deps)` projector at `src/wallet/infrastructure/adapters/inbound/http/types.ts` that populates `asyncDispatch` only when BOTH `resultSubscriber` is wired AND `config.asyncPipeline` is set — either missing → handler falls back to today's inline sync dispatch even when the rollout flag was flipped early. Six handlers refactored (`deposit`, `withdraw`, `charge`, `adjustBalance`, `transfer`, `captureHold`): when async-eligible, serialise the command into `queue_payload` (BigInt → string, conditional spreads so missing optional fields aren't sent as undefined), call `asyncDispatch<TResult>(...)`, then map outcomes — `completed` → 201 with the same body shape as the sync path (cast from `TResult`); `pending` → 202 `{ movement_id, status: "pending" }`; `failed` → `throw AppError.domainRule("MOVEMENT_FAILED", failedReason)` so the global onError maps to 422. `captureHold` additionally pre-resolves `wallet_id` via `queryBus.dispatch(GetHoldQuery(holdId, ctx.platformId))` so the cross-tenant guard happens at the handler boundary; the resolved `wallet_id` flows into `queue_payload.walletId` and the worker's hydrator never re-queries. Routes (`wallets`, `transfers`, `holds`) call `buildMutationDeps(deps)` once and pass the same projection. Known limitation: failed outcomes uniformly surface as 422 `MOVEMENT_FAILED` (the original AppError kind/code is lost across the queue) — a future iteration can extend `MovementResult` with `failedKind`/`failedCode` for full fidelity. Unit: 1127/1127 at 100% coverage (9 new asyncDispatch cases + 3 buildMutationDeps cases + 12 per-handler async-branch cases + existing sync tests updated). E2E: 277/277 unchanged (async-path E2E deferred — requires JWT-forging in harness).
- [x] **Async movement processing — Phase 2B.7a (foundation for the handler refactor):** `MovementResult` gains optional `body?: Record<string, unknown>` so the worker can publish the matching service's return value alongside the terminal status (the awaiting HTTP handler then rebuilds the same 200 shape as the sync path — failures still carry only `failedReason`). `ProcessMovementUseCase.dispatchByType` threads each `<op>Service.execute(...)` return value back through to `resultPublisher.publish({ status: "posted", body })`; all six service result types carry plain string IDs so the loose record round-trips through JSON without custom serialisers. New top-level config flag `asyncProcessingEnabled: boolean` (driven by `WALLET_ASYNC_PROCESSING_ENABLED`, strict `"true"`/`"false"` enum so a typo cannot silently disable rollout); independent of `asyncPipeline` wiring so a deployment can flip the flag before publisher env vars are set. `wiring.ts` pub/sub block reorganised: a single dedicated ioredis client backs both `RedisResultPublisher` (wired when QStash signing keys are present — this process can act as a worker) and `RedisResultSubscriber` (wired when `asyncProcessingEnabled=true` — this process serves HTTP handlers that may opt into async). `Dependencies` gains optional `resultSubscriber?: RedisResultSubscriber`. Unit: 1096/1096 at 100% coverage (new posted-with-body round-trip cases on publisher + subscriber, usecase body capture, 6 new config cases covering missing/`"true"`/`"false"`/non-boolean/independence-from-pipeline). E2E: 277/277 unchanged.
- [x] **Async movement processing — Phase 2B.6 (worker dispatcher real body):** new CQRS triple under `src/wallet/application/worker/processMovement/` (outside `application/command/` because the worker is a cross-cutting dispatcher that needs all six operation commands + services — the use-case isolation check forbids that under `command/`): `command.ts` (`ProcessMovementCommand(movementId)` + `ProcessMovementResult { outcome: "posted" | "failed" | "noop", failedReason? }`), `payload.ts` (six pure per-type hydrators reading `Movement.queue_payload` — BigInt amounts travel as strings, validated and decoded; the transfer hydrator rejects self-transfers at the boundary; the `hold_capture` hydrator reads a pre-resolved `walletId` so the worker never does a synchronous hold→wallet lookup on every queued message), and `usecase.ts` (orchestrator: `markProcessing` atomic claim → `null` returns outcome `noop` without publishing; hydrate per-type command + derive lock keys; run `<op>Service.execute` inside `lockRunner.run(lockKeys, txManager.run(...))` with `markPosted` **inside the same tx** for atomicity; `resultPublisher.publish({ posted })` OUTSIDE the lock so a Redis blip cannot block release; on any thrown error: best-effort `markFailed` + `publish({ failed, failedReason })`, each wrapped in their own try/catch so a Redis blip after a DB blip does not mask the root cause; transient retries deferred). All six `MovementType` values dispatched (`deposit`, `withdrawal`, `charge`, `adjustment`, `transfer`, `hold_capture`). `RedisResultPublisher` wired in `wiring.ts` with a dedicated ioredis client (2s commandTimeout vs the lock client's 500ms fall-through profile); gate `config.qstash && config.walletLock?.transport === "tcp"` since Upstash REST does not support pub/sub. `SharedInfra.resultPublisher?` optional; `wallet.module.ts` instantiates and registers `ProcessMovementCommand` only when wired. Worker handler refactored to dispatch via the command bus (`processMovementRoute(commandBus, idGen, logger)`) — returns 200 with `{ ok, movement_id, outcome, failed_reason? }` regardless of outcome because QStash only retries on non-2xx and a terminal result must never be retried. Unit: 1089/1089 at 100% coverage (28 new hydrator cases + 26 new use case orchestration cases + 7 refactored handler cases). E2E: 277/277 unchanged.
- [x] **Async movement processing — Phase 2B.5 (`EnqueueMovementUseCase` + publisher wiring):** new CQRS triple under `src/wallet/application/command/enqueueMovement/`: `command.ts` (`EnqueueMovementCommand(type, platformId, idempotencyKey, queuePayload, reason?)` + `EnqueueMovementResult { movementId }`) and `usecase.ts`. The handler generates a fresh `movement_id`, builds `Movement.create({ status: "pending", platformId, queuePayload, reason })`, persists it via `movementRepo.save`, then forwards `{ movementId, idempotencyKey }` to `IMovementQueuePublisher.publish`. Save-before-publish is invariant — a delivered queue message must always find a row. If publish fails after save, the pending row stays as a known orphan for a future reconciliation job (extending the state machine to allow `pending → failed` rollback is deferred). No lock + tx envelope here because the only DB write is a single idempotent INSERT and the publish is external; the async-path service (which does need lock + tx) runs later in the worker adapter. `Config` gains an optional `asyncPipeline` block built only when `QSTASH_TOKEN`, `QSTASH_URL`, and `WALLET_INTERNAL_WORKER_URL` are all set; new env vars `WALLET_QSTASH_QUEUE_NAME` (default `"wallet-movements"`) and `WALLET_HANDLER_WAIT_MS` (default `1500`). `SharedInfra` gains optional `movementQueuePublisher?: IMovementQueuePublisher`; `wiring.ts` instantiates `QStashClient` + `QStashMovementQueuePublisher` gated by `config.asyncPipeline`. `wallet.module.ts` conditionally registers `EnqueueMovementCommand.TYPE → handler` on the command bus only when the publisher is present (consistent with the optional `lockRunner` pattern) — when unwired, dispatching the command fails-fast at the bus and the Phase 2B.6 handler refactor will fall back to the sync path. Unit: 1033/1033 at 100% coverage (6 new EnqueueMovementUseCase tests + 6 new config permutations). E2E: 277/277 unchanged.
- [x] **Async movement processing — Phase 2B.4 (`Movement.queuePayload` for worker replay):** new nullable `movements.queue_payload jsonb` column. Migration `20260512053811_add_movement_queue_payload` is additive (no backfill needed — legacy rows stay NULL because they were synchronous and never enqueued anything). `Movement` aggregate carries a `MovementQueuePayload | null` field (loose `Record<string, unknown>` — Phase 2B.5/6 (de)serialise narrowly by `movement.type`); optional in `create`, required in `reconstruct`, preserved across all three state transitions. `PrismaMovementRepo.save` persists it; `findById` / `markProcessing` reload it. DB trigger extended to block any UPDATE on `queue_payload` (verified with raw SQL). Unit: 1021/1021 at 100% coverage. E2E: 277/277.
- [x] **Async movement processing — Phase 2B.3 (outbound adapter implementations):** `QStashMovementQueuePublisher` (implements `IMovementQueuePublisher`) uses `@upstash/qstash` `Client.queue(name).enqueueJSON({ url, body, deduplicationId? })` with a minimal `{ movement_id }` body and forwards `idempotencyKey` as `deduplicationId`. `RedisResultPublisher` (implements `IResultPublisher`) does SET (60-second TTL, durable cache) + PUBLISH (fast-path wake-up) in that order to close the late-subscriber race. New `RedisResultSubscriber` exposes `waitFor(movementId, { timeoutMs, pollMs? })` — polling-based GET loop chosen for serverless compatibility (no per-request long-lived connection); transient Redis errors during a poll are swallowed so the caller observes a timeout and falls back to 202 even if the backend stays unhealthy for the full window. No wiring yet (Phase 2B.6 registers the adapters and reads `QSTASH_TOKEN`, `QSTASH_URL`, `WALLET_INTERNAL_WORKER_URL`, `WALLET_HANDLER_WAIT_MS`). Unit: 1017/1017 at 100% coverage. E2E: 277/277.
- [x] **Async movement processing — Phase 2B.2 (Movement state machine + repo write methods):** `Movement` aggregate gains immutable `transitionToProcessing()`, `transitionToPosted()`, `transitionToFailed(reason)` (each returns a new aggregate). Invalid transitions throw `INVALID_MOVEMENT_TRANSITION`. `IMovementRepository` gains `findById` (platform-scoped, NULL-platform legacy orphans intentionally not resolvable), `markProcessing` (atomic claim via `updateMany WHERE id AND status='pending'`; returns null when another worker won the race), `markPosted` and `markFailed` (guarded by `WHERE status='processing'`, throws `MOVEMENT_NOT_FOUND` if no row matches). `PrismaMovementRepo` implements them. DB-level enforcement updated in `prisma/immutable_ledger.sql`: the old "block all UPDATE" trigger on `movements` is replaced by `movements_lifecycle_only_update` (column-aware — allows only `status` along the state machine and `failed_reason`) + `movements_no_delete`. The startup safety-net check in `src/index.ts` updated for the new trigger names. Unit: 1000/1000 at 100% coverage. E2E: 277/277.
- [x] **Async movement processing — Phase 2B.1 (denormalise `Movement.platformId`):** new nullable column `movements.platform_id` + `(platform_id, status, created_at)` index. Migration `20260512044537_add_movement_platform_id` is self-contained: ensures the `prevent_immutable_modify` function exists, drops the `movements_immutable` trigger so the one-time backfill UPDATE runs (the trigger blocks UPDATEs by design), populates `platform_id` from `transactions.wallet.platform_id` via `SELECT DISTINCT ON (t.movement_id)`, then restores the trigger. Domain: `Movement.create()` requires `platformId`; `Movement.reconstruct()` accepts `platformId: string | null` so legacy orphan rows (movements without transactions, ~40k of 87k in the prod backup) coexist as `null`. All 7 sync use cases pass `cmd.platformId`. `PrismaMovementRepo.save` persists the column. `PrismaMovementReadStore.getById` switches the filter to `OR: [{ platformId }, { transactions: { some: { wallet: { platformId } } } }]` — direct match for new rows (incl. Phase 2B.2 `pending`/`processing` movements), transactional fallback for legacy NULL orphans. API contract unchanged. Unit: 976/976 at 100% coverage. E2E: 277/277.
- [x] **Async movement processing — Phase 2A (service extraction across all 7 mutating operations):** every command use case is now split into a thin orchestrator (`usecase.ts`: lock + tx envelope + `Movement.create` + `movementRepo.save` + delegate) and a colocated business-rule service (`service.ts`: wallet validation, balance updates, transactions, ledger entries, idempotent saves). Affected operations: `deposit`, `withdraw`, `transfer`, `charge`, `adjustBalance`, `captureHold`, `importHistoricalEntry`. Each service receives `(ctx, cmd, movement)` and assumes the caller has already acquired the lock, opened the transaction, and persisted the Movement — the contract the Phase 2B async worker will satisfy by claiming a pending Movement and invoking the same service. Existing API contract intact; runtime behaviour identical. Tests split per operation: `<op>.usecase.test.ts` covers orchestration only (mocks the service); `<op>.service.test.ts` carries the full business-rule coverage (mocks the repos). AGENTS.md § "CQRS file structure" updated to document the three-file pattern. Unit: 974/974 at 100% coverage. E2E: 277/277 unchanged.

## What's Left to Build

- [ ] **Async movement processing — Phase 3+**: (a) E2E coverage for the async path — at minimum happy + claim-race + timeout-to-202 cases (requires forging an Upstash-Signature JWT inside the harness against the dev keys); (b) per-platform rollout mechanism — replace the global `WALLET_ASYNC_PROCESSING_ENABLED` with a `platforms.async_processing_enabled boolean` column or a percentage knob so async rolls out tenant-by-tenant; (c) transient-error retries in `ProcessMovementUseCase` — today any error marks `failed`; a future iteration distinguishes transient (Prisma write conflict, Redis blip) from terminal (domain error) and lets QStash retry up to N times; (d) reconciliation job for `pending` movements whose QStash publish failed (today they stay as known orphans); (e) full error fidelity across the queue — extend `MovementResult` with `failedKind`/`failedCode` so the handler rebuilds the original AppError and the async path returns the same HTTP status as the sync path on domain rejections (today both surface as 422 `MOVEMENT_FAILED`). See `HIGH_CONCURRENCY_PLAN.md`.
- [ ] Platform bounded context: remaining platform management features (suspend, revoke, API key rotation)
- [ ] Platform API (endpoints auto-documented via hono-openapi once implemented)
- [ ] Body size limit middleware (64KB)
- [ ] Status CHECK constraints in PostgreSQL (wallets, holds, transactions, ledger_entries)
- [ ] Idempotency keys scoped by platform — UNIQUE(idempotencyKey, platformId)
- [ ] Rate limiting middleware per platformId
- [ ] Graceful shutdown (SIGTERM cleanup)
- [ ] Wallet lookup by owner endpoint — `GET /v1/wallets?owner_id=...&currency_code=...`
- [ ] Metadata field on mutation endpoints (deposit, withdraw, transfer, adjust)
- [ ] Hash chain tamper detection (ledger entries)
- [ ] Reconciliation background job (Wallet internal: cached_balance vs SUM(ledger))
- [x] Server-side retry for VERSION_CONFLICT (3 attempts + exponential backoff in PrismaTransactionManager)
- [ ] Production deploy configuration (managed PostgreSQL + Node.js process)
- [ ] Integration tests
- [x] Idempotency record TTL cleanup job (60s interval) — implemented as command dispatched via bus
- [x] API documentation (auto-generated OpenAPI + Scalar UI at /docs)
- [x] ~~System wallet contention~~ — closed by sharding (see above). Residual 409s under extreme load are expected SERIALIZABLE aborts on other rows (user wallet, transaction, ledger index), retryable by the client with the same `Idempotency-Key`.

## Known Issues

- Prisma build scripts need `pnpm approve-builds` on fresh install
- `pnpm.onlyBuiltDependencies` set in package.json for CI compatibility
